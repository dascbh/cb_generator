<%= form_for(@main_panel) do |f| %>
  <% if @main_panel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_panel.errors.count, "error") %> prohibited this main_panel from being saved:</h2>

      <ul>
      <% @main_panel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
